#include "main.h"

/**
 * _strcat - function to concatenates two strings
 * @dest: Destination
 * @src: Source
 * Returns: A pointer
 */

char *_strcat(char *dest, char *src)
{
	int i, n;

	for (i = 0; dest[i] != '\0'; i++)
	{

	}
	for (n = 0; (dest[i + n] = *src++) != '\0'; n++)
	{

	}
	return (dest);
}
