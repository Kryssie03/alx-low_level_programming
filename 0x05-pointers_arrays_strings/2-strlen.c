#include "main.h"

/**
 * _strlen - function to print length of string
 * @s: function parameter
 *
 * Return: 0
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{
		;
	}
	return (0);
}
