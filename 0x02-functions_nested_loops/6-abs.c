#include "main.h"
/**
 * _abs - funtction that computes the absolute values of intger
 * @c: arguement of the function
 *
 *  Return: 0
 */
int _abs(int)
{
	if (c > 0 || c == 0)
	{
	return (c);
	}
	else 
	return (c * -1);
}
